package components

templ Username(value *string) {
	{{
		author, color := "anonymous", "text-content-muted"
		if value != nil {
			author = "@" + *value
			color = "text-complementary"
		}
	}}
	<span class={ color }>{ author }</span>
}
