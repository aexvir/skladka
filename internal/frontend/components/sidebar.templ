package components

import (
	"fmt"
	"github.com/aexvir/skladka/internal/auth"
	"github.com/aexvir/skladka/internal/frontend/icons"
	"github.com/aexvir/skladka/internal/paste"
	"strconv"
)

templ Sidebar() {
	<div class="h-[90vh] lg:h-full w-full lg:w-[300px] flex-none lg:border-l border-content-muted/30 fixed inset-x-0 -bottom-full lg:static transition-all duration-300" id="sidebar">
		<div class="h-full">
			<form method="POST" action="/" class="h-full bg-surface p-4 flex flex-col lowercase rounded-t-2xl lg:rounded-none shadow-xl lg:shadow-none" id="paste-form">
				<div class="flex-grow space-y-4">
					@TextInput("title", "title", "", icons.Paperclip(14, 14, "text-content-muted"))
					@TagsInput("tags", "tags", icons.Tag(14, 14, "text-content-muted"))
					@SelectInput("syntax", "syntax highlight", icons.Code(14, 14, "text-content-muted"), "plaintext", "go", "python", "yaml", "hcl", "ocaml", "json", "yaml", "xml", "rust", "zig")
					@ToggleWithContent("toggle-password", "", "password protection", icons.Lock(14, 14, "text-content-muted")) {
						@PasswordInput("password", "password", "")
					}
					@ToggleWithContent("toggle-expiration", "", "expiration", icons.Clock(14, 14, "text-content-muted")) {
						@SelectInput("expiration", "", icons.Clock(14, 14, "text-content-muted"), "no expiration", "1m", "5m", "10m", "30m", "1h", "1d", "5d", "30d", "1y")
					}
					@Toggle("toggle-unlisted", "unlisted", "unlisted", icons.Eye(14, 14, "text-content-muted"))
					<input type="hidden" name="content" id="editor-content"/>
				</div>
				<div class="flex flex-row w-full fixed inset-x-0 bottom-0 lg:static p-4 lg:p-0">
					<button type="submit" class="w-full bg-accent text-accent-contrast py-2 rounded-l lg:rounded transition-all duration-200 hover:bg-accent-muted hover:shadow-md">
						<span class="flex-grow">paste</span>
					</button>
					<div class="w-12 flex items-center justify-center border-l border-content-muted/30 cursor-pointer lg:hidden rounded-r bg-accent" id="expand-sidebar">
						@icons.ChevronUp(18, 18, "text-accent-contrast transform transition-all duration-200")
					</div>
				</div>
			</form>
		</div>
		@initSidebar()
	</div>
}

templ Metadata(user *auth.User, paste paste.Paste) {
	<div class="h-[90vh] lg:h-full lg:w-[300px] flex-none lg:border-l border-content-muted/30 fixed inset-x-0 -bottom-full lg:static transition-all duration-300" id="metadata-sidebar">
		<div class="h-full">
			<div class="h-full bg-surface flex flex-col p-4 rounded-t-2xl lg:rounded-none shadow-xl lg:shadow-none">
				<div class="flex-grow">
					<h1 class="w-full text-2xl text-content inline-flex items-center justify-between" id="paste-title">
						{ paste.Title }
						if !paste.Public {
							<div class="flex-0">
								@icons.Eye(18, 18, "text-content-muted")
							</div>
						}
						if paste.Password != nil {
							<div class="flex-0">
								@icons.Lock(18, 18, "text-content-muted")
							</div>
						}
					</h1>
					<div class="flex flex-row w-full text-sm gap-2">
						@Username(paste.Owner)
						·
						<span class="text-content-muted flex flex-row items-center gap-2">
							@icons.Calendar(14, 14, "text-content-muted")
							{ paste.Creation.Format("Jan 2, 2006") }
						</span>
					</div>
					<div class="flex flex-row justify-center gap-2 py-4 text-sm">
						{{ size := fmt.Sprintf("%.1f", float64(len([]byte(paste.Content)))/1024.0) }}
						{ strconv.Itoa(paste.Views) } views | { paste.Syntax } | { size }kb
					</div>
					if len(paste.Tags) > 0 {
						<div class="space-y-1">
							<label class="text-sm flex flex-row gap-2 items-center">
								@icons.Tag(14, 14, "text-content-muted")
								tags
							</label>
							<div class="flex flex-wrap gap-1">
								for _, tag := range paste.Tags {
									<span class="bg-surface-muted text-content-muted border border-content-muted/30 px-2 py-0.5 rounded text-sm">{ tag }</span>
								}
							</div>
						</div>
					}
					if paste.Expiration != nil {
						<div class="w-full mt-4 border-t border-content-muted/30"></div>
						<div class="flex flex-row justify-center gap-2 py-4 text-red-400">
							expires { paste.Expiration.Format("Jan 2, 2006") }
						</div>
					}
				</div>
				if paste.Owner != nil && user != nil && *paste.Owner == user.Username {
					<button onclick="deletePaste()" type="button" class="w-full text-center bg-error hover:bg-error/70 text-white px-4 py-2 mb-2 rounded hover:shadow-md transition-all duration-200 whitespace-nowrap">
						delete
					</button>
				}
				<div class="flex flex-row w-full fixed inset-x-0 bottom-0 lg:static p-4 lg:p-0">
					<div class="w-full flex flex-row border border-content-muted/30 rounded lg:border-none shadow-md lg:shadow-none rounded-r-none">
						<button onclick="window.Editor.copyToClipboard()" type="button" class="rounded-l w-full text-center bg-surface-muted hover:bg-accent text-content hover:text-accent-contrast px-4 py-2 hover:shadow-md transition-all duration-200 whitespace-nowrap">copy</button>
						<a href={ templ.URL(fmt.Sprintf("/r/%s", paste.Reference)) } class="w-full text-center bg-surface-muted hover:bg-accent text-content hover:text-accent-contrast px-4 py-2 hover:shadow-md transition-all duration-200 whitespace-nowrap">raw</a>
						<button onclick="window.Editor.downloadAsFile()" type="button" class="rounded-r w-full text-center bg-surface-muted hover:bg-accent text-content hover:text-accent-contrast px-4 py-2 hover:shadow-md transition-all duration-200 whitespace-nowrap">download</button>
					</div>
					<button onclick="toggleMetadata()" type="button" class="w-12 flex items-center justify-center border border-l-0 border-content-muted/30 cursor-pointer lg:hidden rounded-r bg-surface-muted" id="expand-metadata">
						@icons.ChevronUp(18, 18, "text-content transform transition-all duration-200")
					</button>
					<script>
						const toggleMetadata = () => {
							const sidebar = document.getElementById('metadata-sidebar')
							const expandbtn = document.getElementById('expand-metadata')
							const expandicon = expandbtn.querySelector('svg')

							sidebar.classList.toggle('expanded')

							if (sidebar.classList.contains('expanded')) {
								sidebar.style.bottom = '0'
								expandicon.style.transform = 'rotate(180deg)'
								return
							}

							sidebar.style.bottom = '-100%'
							expandicon.style.transform = 'rotate(0deg)'
						}

						const deletePaste = () => {
							if (!confirm('Are you sure you want to delete this paste?')) {
								return
							}

							fetch(window.location.pathname, { method: 'DELETE' })
								.then(
									response => {
										if (response.status < 400) {
											window.location.href = '/'
											return
										}
										alert('Failed to delete paste')
									}
								)
						}
					</script>
				</div>
			</div>
		</div>
	</div>
}

script initSidebar() {
    document.addEventListener(
        'DOMContentLoaded', () => {
            const sidebar = document.getElementById('sidebar')
            const expandbtn = document.getElementById('expand-sidebar')
            const expandicon = expandbtn.querySelector('svg')

            let sidebarExpanded = false

            const toggleSidebar = () => {
                sidebarExpanded = !sidebarExpanded
                sidebar.style.bottom = sidebarExpanded ? '0' : '-100%'
                expandicon.style.transform = sidebarExpanded ? 'rotate(180deg)': 'rotate(0deg)'
            }

            expandbtn.addEventListener(
                'click', (e) => {
                    e.stopPropagation()
                    toggleSidebar()
                }
            )

            document.
                querySelector('select[name="syntax"]').
                addEventListener(
                    'change', (e) => {
                        window.Editor.setSyntax(e.target.value)
                    }
                )

            document.
                querySelector('form').
                addEventListener(
                    'submit', (e) => {
                        const editorContent = window.Editor.getContent()
                        document.getElementById('editor-content').value = editorContent
                    }
                )
        }
    )
}
